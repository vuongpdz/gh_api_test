name: Cleanup-PR
on:
  pull_request:
    types:
      - closed
      - labeled
  workflow_dispatch:
jobs:
  main-auto-merge:
    runs-on: self-hosted
    steps:
      - if: github.event.pull_request.state == 'closed' && contains(github.event.pull_request.labels.*.name, 'delete-after-closed')
        name: Cleanup closed PR labeled delete-after-closed
        uses: dawidd6/action-delete-branch@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branches: ${{ github.event.pull_request.number }}
